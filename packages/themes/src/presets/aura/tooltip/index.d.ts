import type { TooltipTokenSections } from '@openuxkit/themes/types/tooltip';

export * from '@openuxkit/themes/types/tooltip';

declare const root: TooltipTokenSections.Root;
declare const colorScheme: TooltipTokenSections.ColorScheme;
declare const _default: {
    root: TooltipTokenSections.Root;
    colorScheme: TooltipTokenSections.ColorScheme;
};

export { colorScheme, _default as default, root };
