{
    "name": "@openuxkit/themes",
    "version": "1.0.0",
    "author": "OpenVi Foundation",
    "description": "Theme presets and theming API for OpenVue and other OpenVi Foundation projects",
    "keywords": ["themes", "css", "style utilities", "design tokens", "openvue", "openux", "openuxkit"],
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/openvi-foundation/openux.git",
        "directory": "packages/themes"
    },
    "bugs": {
        "url": "https://github.com/openvi-foundation/openux/issues"
    },
    "homepage": "https://github.com/openvi-foundation/openux#readme",
    "main": "./dist/index.mjs",
    "module": "./dist/index.mjs",
    "types": "./dist/index.d.mts",
    "exports": {
        ".": {
            "types": "./dist/index.d.mts",
            "import": "./dist/index.mjs",
            "default": "./dist/index.mjs"
        },
        "./types": {
            "types": "./types/index.d.ts"
        },
        "./types/*": {
            "types": "./types/*/index.d.ts"
        },
        "./tokens": {
            "types": "./tokens/index.d.mts",
            "import": "./tokens/index.mjs",
            "default": "./tokens/index.mjs"
        },
        "./*": {
            "types": "./dist/*/index.d.ts",
            "import": "./dist/*/index.mjs",
            "default": "./dist/*/index.mjs"
        }
    },
    "publishConfig": {
        "access": "public"
    },
    "sideEffects": false,
    "files": ["dist", "umd", "tokens", "types", "README.md", "LICENSE"],
    "scripts": {
        "build": "cross-env NODE_ENV=production INPUT_DIR=src/ OUTPUT_DIR=dist/ pnpm run build:package",
        "build:dev": "cross-env NODE_ENV=development INPUT_DIR=src/ OUTPUT_DIR=dist/ pnpm run build:dev:package",
        "build:package": "pnpm run type:check && pnpm run build:tokens && tsup && pnpm run build:postbuild",
        "build:dev:package": "pnpm run build:tokens && tsup --watch && pnpm run build:postbuild",
        "build:postbuild": "node ./scripts/postbuild.mjs",
        "build:tokens": "node ./scripts/build-tokens.mjs",
        "type:check": "tsc --noEmit",
        "dev:link": "pnpm link --global && npm link"
    },
    "dependencies": {
        "@openuxkit/styled": "workspace:^"
    }
}
